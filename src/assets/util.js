export const createEle = tag => {
  return document.createElement(tag)
}

export const byId = id => {
  return document.getElementById(id)
}

export const createDF = () => {
  return document.createDocumentFragment()
}
